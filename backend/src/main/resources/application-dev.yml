spring:
  datasource:
    url: jdbc:sqlserver://${DB_HOST:localhost}:${DB_PORT:1433};databaseName=${DB_NAME:zentrix};encrypt=true;trustServerCertificate=true
    username: ${DB_USER:sa}
    password: ${DB_PASSWORD:Zentrix_Dev_2026!}
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

zentrix:
  cors:
    allowed-origins: ${ZENTRIX_CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:3100}
  jwt:
    secret: ${ZENTRIX_JWT_SECRET:dev-only-secret-do-not-use-in-staging-or-prod-0123456789}
  security:
    encryption-key: ${ZENTRIX_ENCRYPTION_KEY:5wYYAh1IGo8WzWpCyjxwWM5/LmMsR7FFF328IRYYwJI=}
  storage:
    apk-dir: ${ZENTRIX_APK_DIR:./data/apks}
  bootstrap:
    admin-email: ${ZENTRIX_BOOTSTRAP_ADMIN_EMAIL:admin@zentrix.local}
    admin-password: ${ZENTRIX_BOOTSTRAP_ADMIN_PASSWORD:Zentrix_Admin_2026!}
